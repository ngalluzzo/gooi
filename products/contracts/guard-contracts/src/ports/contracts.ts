/**
 * Canonical ports contract API for @gooi/guard-contracts.
 */
export type {
	SemanticJudgePort,
	SemanticJudgeResult,
} from "./semantic-judge-port";

export const portsContracts = Object.freeze({});
