/**
 * Canonical boundary contract API.
 */
import * as policy from "./policy";

export const artifactAlignmentContracts = Object.freeze({
	areBindingArtifactsAligned: policy.areBindingArtifactsAligned,
});
